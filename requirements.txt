fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
pydantic==2.5.0
azure-ai-documentintelligence==1.0.0b1
azure-search-documents==11.4.0
azure-core==1.29.5
openai==1.3.0
python-multipart==0.0.6
PyPDF2==3.0.1

semantic-kernel==1.0.3
azure-ai-projects==1.0.0b4
azure-ai-inference==1.0.0b4
azure-identity==1.15.0
asyncio-mqtt==0.16.2

# Enhanced AI capabilities
langchain==0.1.20
langchain-openai==0.1.8
langchain-community==0.0.38

# State management and validation
redis==5.0.1
jsonschema==4.21.1
typing-extensions==4.9.0

# Microsoft Agent Framework (pre-release)
# IMPORTANT: This package requires --pre flag during installation
# Install with: pip install agent-framework --pre
# Or install all requirements with: pip install -r requirements.txt --pre
agent-framework

# Optional: tiktoken for accurate token counting
# If installation fails (e.g., requires Rust compiler on Python 3.14+), 
# the code will automatically use token estimation instead.
# To install tiktoken manually (if you have Rust or prebuilt wheels):
# pip install tiktoken
# Otherwise, skip it - the chunking service works fine with estimation.